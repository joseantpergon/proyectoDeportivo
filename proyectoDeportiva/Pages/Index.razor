@page "/"

<MudSnackbar @bind-Open="@mostrarSnackbar" Message="Operación exitosa" Severity="Severity.Success" />

@code {
    private bool mostrarSnackbar;

    private void mostrarSnackbar(bool isOpen)
    {
        mostrarSnackbar = isOpen;
    }
}